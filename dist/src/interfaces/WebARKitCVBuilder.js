export class WebARKitBase {
    width;
    height;
    trackable;
    isLoaded;
    opencv;
    constructor(width, height, trackable, isLoaded, opencv) {
        this.width = width;
        this.height = height;
        this.trackable = trackable;
        this.isLoaded = isLoaded;
        this.opencv = opencv;
    }
}
//# sourceMappingURL=WebARKitCVBuilder.js.map